server:
  port: 20280
spring:
  application:
    name: keensense-task
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST}
        enabled: true
      config:
        file-extension: yaml
        server-r-addr: ${NACOS_HOST}
        shared-dataids: service-monitor.yaml,commonip.properties
        refreshable-dataids: service-monitor.yaml,commonip.properties
logging:
  config: /logs/config/logback-spring.xml
