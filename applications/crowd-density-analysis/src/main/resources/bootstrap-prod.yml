spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:9848
        enabled: true
      config:
        file-extension: yaml
        server-addr: localhost:9848
        shared-dataids: keensense-densecrowd-prod.yaml
        refreshable-dataids: keensense-densecrowd-prod.yaml
logging:
  #config: /logs/config/logback-spring.xml
  config: classpath:logs/config/logback-spring.xml
